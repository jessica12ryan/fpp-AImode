<?php
// callbacks.php — minimal callbacks for FPP live load (§2.8)
// This plugin has no commands/descriptions.json, so live reload is not strictly needed,
// but the file gates PLUGIN load so the API routes become active without fppd restart.
?>
